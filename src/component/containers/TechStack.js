import React from 'react';
import styled from 'styled-components';

const TechStack = () => {
    return (
        <div>
            <div>
                <h2>What I can:</h2>
               <p>HTML, CSS, JS, React, Npm, Git,</p>
            </div>
        </div>);
}

export default TechStack;